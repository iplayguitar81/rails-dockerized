
<br/>
<div class="row">


<div class="col-12">
    <% @news.each do |news| %>
  
<div class="row">
        <div class="col-lg-6 col-md-12 col-sm-12 col-12 order-1 order-lg-1 order-sm-1">
            <%= image_tag news.cover_picture.variant( charcoal: ".10", border: "5%x5%", flop: true), :class => "img-fluid" %>
        </div>

        <div class="col-lg-6 col-md-12  col-sm-12 col-12 order-2 order-lg-2 order-sm-2">
            <span class=""><%= news.published.strftime("%B %d, %Y") %></span>
            <br>
            <br>
            <h2 class="verlag-bold"><%= news.title.titleize %></h2>
            <h3 class="text-muted"><%= news.subtitle.titleize %></h5>
            <h4><%= news.published.strftime("%B %d, %Y") %></h4>
            <p class="">
			<%= news.body %>
		    </p>
            <p>
			    <%= link_to 'Read More...', news, :class => 'btn btn-dark' %>
		    </p>
        </div>
    </div>
		<br/>
        <hr/>


    <% end %>
</div>






</div>
